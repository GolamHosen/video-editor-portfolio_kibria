// Legacy database schema exports redirecting to MongoDB Mongoose models
export {
  Category as categories,
  Project as projects,
  Media as media,
  Testimonial as testimonials,
  Service as services,
  SiteSetting as siteSettings,
  ContactSubmission as contactSubmissions,
  AdminUser as adminUsers,
} from "@/models";
